import 'package:flutter/material.dart';

class FurusatoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FurusatoPage'),
      ),
      body: Container(),
    );
  }
}
